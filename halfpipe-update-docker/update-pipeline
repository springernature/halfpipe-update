#!/usr/bin/env bash
set -euo pipefail

if [ "${CONCOURSE_TEAM}" == "engineering-enablement" ]; then
  echo "running update-pipeline-beta"
  update-pipeline-beta
else  
  echo "running update-pipeline-stable"
  update-pipeline-stable
fi
